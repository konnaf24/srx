# Session artifacts

Diagrams and a transcript produced while building the two-host lab and the
`deploy/` tooling. Reference material — not part of the probe runtime.

| File | What it shows |
| --- | --- |
| `srx_design_goal.svg` | The repository's design goal: telemetry validation, the core loop, and the four functional layers. |
| `srx_implementation_steps.svg` | The idea → spec → build → test → commit → enhance vibe-coding workflow. |
| `srx_prompt02062026.svg` | Deployment & workload steps across the generator (`.46`) and target (`.45`) hosts. |
| `srx_prompt02062026.md` | Written transcript of the deployment/workload session. |

Open the `.svg` files in any browser or image viewer.
