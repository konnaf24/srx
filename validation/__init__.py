"""Validation layer for the SRX detection probe.

Contains the pure-Python correlation engine (`correlator`) and assertion
helpers (`assertions`). These modules have **no** dependency on live
infrastructure and are fully unit-testable offline.
"""
